{
  "result": "Implement a `watch` mode that automatically detects file changes in `notes/`, rebuilds the link/task index, and refreshes the web UI without restarting the app. \n\nI couldn’t run `go test` here because the `go` toolchain is not installed in this environment."
}
